import React from 'react';

const Header: React.FC = () => {
    return (

        <header
            className="flex w-full items-center justify-center bg-stone-900 py-7 text-3xl font-bold text-white h-[50px]">
            <div>
                University Finder
            </div>
        </header>
    );
};

export default Header;
